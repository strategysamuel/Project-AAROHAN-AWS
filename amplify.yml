version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build --workspace customer-portal
  artifacts:
    baseDirectory: apps/customer-portal/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*